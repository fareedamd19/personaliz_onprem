/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['personaliz.s3.ap-south-1.amazonaws.com','dyolkjkaata8s.cloudfront.net'],
      },
};

export default nextConfig;
